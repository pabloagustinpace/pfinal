import React from 'react';
import DetailChef from '../../containers/chefDetail';

const detChefs = (props) => {
    return <React.Fragment> <DetailChef {...props} /> </React.Fragment>
}

export default detChefs;